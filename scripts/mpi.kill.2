#!/bin/bash
if [ "x${SOS_ENV_SET}" == "x" ] ; then
  echo "Please set up your environment for SOS.  (source <sos_root>/hosts/<hostname>/setenv.sh)"
  kill -INT $$
fi
echo ""; echo "*** Launching the sosd_stop tool..."; echo ""
$SOS_BUILD_DIR/bin/sosd_stop
killall sosa
killall sosd

